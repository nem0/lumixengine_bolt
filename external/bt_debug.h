#pragma once

#if __cplusplus
extern "C" {
#endif

#include "bt_parser.h"

/**
 * Dumps a textual representation of the parse tree generated by `parser` to stdout
 * Useful for debugging parsing errors.
 * Automatically called during module compilation if `BOLT_PRINT_DEBUG` is defined.
 */
BOLT_API void bt_debug_print_parse_tree(bt_Parser* parser);

/**
 * Dumps a disassembled version of `function` into a string and returns it.
 * Reconstructs all mnemonics and register accesses.
 * Useful to debug compiler errors.
 */
BOLT_API bt_String* bt_debug_dump_fn(bt_Context* ctx, bt_Callable* function);

#if __cplusplus
}
#endif